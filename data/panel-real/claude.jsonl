{"findingId":"f-01","label":"NEEDS_INVESTIGATION","confidence":75,"reasoning":"Q1 fails on certainty: TP for storing 2FA backup codes reversibly requires confirming symmetricEncrypt is true reversible encryption with a separately-stored key (cited code shows symmetricDecrypt round-tripping, consistent with that), AND that the threat model treats DB+key compromise as in-scope. The hash-vs-encrypt tradeoff is a real but debatable design choice; need the symmetricEncrypt impl and key-storage location to confirm the codes are recoverable rather than this being defensible 2FA-c","model":"claude","vendor":"claude"}
{"findingId":"f-02","label":"TP","confidence":82,"reasoning":"Q1: cited `bookingRefsFiltered.forEach(async (bookingRef) => {...await getCalendar...return calendar?.deleteEvent...})` is the textbook forEach-async anti-pattern. forEach discards the returned promises, so rejections from getCalendar/deleteEvent are never awaited or propagated to the caller — a concrete runtime defect (lost errors, fire-and-forget). Confirmable from the snippet's language semantics alone; the surrounding try/catch (Q3) cannot catch floating-promise rejections either way. Produc","model":"claude","vendor":"claude"}
{"findingId":"f-03","label":"TP","confidence":80,"reasoning":"Q1 confirms the defect as written: forEach(async bookingRef => {... await getCalendar(...)}) — forEach categorically discards the promises its callback returns, so the awaited calendar deletes are never awaited and rejections become unhandled (no try/catch visible at callback top). Confirmable from the snippet alone since forEach's non-awaiting behavior is a JS language guarantee; matches PRACTICES 'ignoring promise rejections' TP example. Production tRPC router.","model":"claude","vendor":"claude"}
{"findingId":"f-04","label":"NEEDS_INVESTIGATION","confidence":72,"reasoning":"Q1: cited code confirms async callback in forEach (anti-pattern that drops Promises). But whether failures are observed depends on unshown code below line 461 — apiDeletes.push(calendar?.deleteEvent(...)) in the prior finding suggests results may be collected/awaited outside the forEach. Need lines 462-472 and the await/Promise.all site to confirm errors are truly swallowed vs. captured in an array.","model":"claude","vendor":"claude"}
{"findingId":"f-05","label":"TP","confidence":88,"reasoning":"Q1 satisfied from cited code alone: `bookingRefsFiltered.forEach(async (bookingRef) => {... await getCalendar...; return calendar?.deleteEvent(...)})`. By language semantics (not unseen code), forEach discards the callback's returned promise, so deleteEvent rejections become unhandled and the outer fn doesn't await completion. This is the PRACTICES rubric's explicit 'ignoring promise rejections' TP example; path is production code (Q4 pass).","model":"claude","vendor":"claude"}
{"findingId":"f-06","label":"TP","confidence":85,"reasoning":"Q1 confirms on the snippet itself: `forEach(async (bookingRef) => { ... await getCalendar(...) })` — forEach discards the promises its async callback returns, so the awaits are never awaited by the caller and any rejection becomes an unhandled rejection that a try/catch wrapping the loop cannot catch. Language semantics are certain; no unshown code needed. Q4 passes: production lib code, not test/vendor. Textbook PRACTICES 'ignoring promise rejections / swallowing errors silently' defect.","model":"claude","vendor":"claude"}
{"findingId":"f-07","label":"TP","confidence":80,"reasoning":"Q1: cited code as written is `forEach(async (bookingRef) => { ... await getCalendar(...) })` — forEach structurally discards callback return values, so the async callbacks' promises float: rejections become unhandled and the async route handler returns synchronously without awaiting deletions. This is intrinsic to forEach+async, not dependent on unseen code. Q4: production tRPC router path. Meets PRACTICES bar (ignored promise rejections / swallowed errors).","model":"claude","vendor":"claude"}
{"findingId":"f-08","label":"TP","confidence":86,"reasoning":"Q1 confirms the defect on the snippet itself: the else-branch is reached only when externalCalendarId is falsy, yet the fallback find predicate `cal.externalId === externalCalendarId` compares against that same falsy value, so it can't match real calendars (truthy externalIds) and `?.externalId` yields undefined — the fallback is dead. Self-contained logic regression in production CalendarService.ts; no unshown code needed to confirm.","model":"claude","vendor":"claude"}
{"findingId":"f-09","label":"TP","confidence":85,"reasoning":"Q1: the defect exists as written. The else branch executes only when `externalCalendarId` is falsy, so `find((cal)=>cal.externalId===externalCalendarId)` searches for a cal matching that falsy value; any match's `?.externalId` is itself undefined, so calendarId is always undefined when no externalCalendarId is passed — defeating the intended destinationCalendar fallback (was `?.externalId`, should be `?.[0]?.externalId`). The ternary itself guarantees reachability of the broken branch; confirmab","model":"claude","vendor":"claude"}
{"findingId":"f-10","label":"NEEDS_INVESTIGATION","confidence":72,"reasoning":"Q3 (downstream/context) unresolved: cited snippet (lines 344-348) shows only the loop start and the conditional findUnique fallback — it does not reveal whether destinationCalendar typically holds many entries, whether COLLECTIVE team events actually populate many uncached credentialIds, or loop size. The N+1 serial-await pattern is plausibly real, but its prod impact depends on the destinationCalendar cardinality and cache-hit rate, which aren't visible in the snippet; would need the full loop ","model":"claude","vendor":"claude"}
{"findingId":"f-11","label":"NEEDS_INVESTIGATION","confidence":65,"reasoning":"Q1: cited loop does call prisma.credential.findUnique per reference inside the `if (!calendarCredential)` fallback, so the N+1 pattern exists as written. But the PRACTICES 'genuinely problematic' bar depends on the max cardinality of bookingCalendarReference (could recurring/team bookings inflate it?) and how often the not-preloaded fallback fires — neither visible in the truncated snippet. The 'slow/timing-out' impact is asserted, not evidenced; for a single booking N is likely small (leaning m","model":"claude","vendor":"claude"}
{"findingId":"f-12","label":"NEEDS_INVESTIGATION","confidence":68,"reasoning":"Cited `?? []` proves destinationCalendar can be empty → `mainHostDestinationCalendar` is undefined → `.integration` (reached only when location===MeetLocationType via && short-circuit) would crash as written. But Q2 (upstream): reachability depends on unshown booking/validation code — whether a Meet-location booking can actually have an empty/null destinationCalendar, or an invariant prevents that co-occurrence. Co-occurrence unconfirmable from snippet → NI, not TP, per principle 5.","model":"claude","vendor":"claude"}
{"findingId":"f-13","label":"NEEDS_INVESTIGATION","confidence":62,"reasoning":"Q1: code at 52-65 does show a real sequential `await client.request` per reminder in a for-loop (Q4: production cron file, not a test). But this PRACTICES 'genuinely problematic' claim is scale-dependent — the cron-timeout failure mode hinges on the max size of remindersToCancel, the cron execution window, and SendGrid rate limits (sequential may be intentional to avoid throttling), none of which are visible. Per Critical principle 5, unconfirmable impact → NEEDS_INVESTIGATION, not TP.","model":"claude","vendor":"claude"}
{"findingId":"f-14","label":"NEEDS_INVESTIGATION","confidence":68,"reasoning":"Q1/Q3: the forEach at 484 calls deleteScheduledEmailReminder/deleteScheduledSMSReminder without await (visible), but the 'errors swallowed, reminders stay active' claim depends on unseen code — whether these fns are async/return rejecting promises and whether they already try/catch+log internally. A floating-promise defect is only real if they're async and unhandled; can't confirm from the snippet. Need the two delete-fn implementations.","model":"claude","vendor":"claude"}
{"findingId":"f-15","label":"TP","confidence":92,"reasoning":"Q1: cited code as written contains the defect. `dayjs(date.start).add(...)` and `dayjs(date.end).add(...)` each return distinct Dayjs object instances; JS `===` does reference comparison on objects, so this is always false regardless of time values. The branch is dead code — intended value comparison (`.isSame()`) never fires. Concrete runtime QUALITY defect confirmable from the snippet alone, no unshown context needed.","model":"claude","vendor":"claude"}
{"findingId":"f-16","label":"TP","confidence":88,"reasoning":"Q1 satisfied on the snippet itself: `start` and `end` have byte-identical RHS (`slotStartTime.hour()*60+slotStartTime.minute()`), yet `end` is named for and compared against `workingHour.endTime`. So `end > workingHour.endTime` actually re-tests the start time; a slot starting in-hours but ending past `endTime` (e.g. 16:30 start, 60-min, end 17:30 vs 17:00 close) is never flagged. Same-line const error, so no upstream/downstream fix (Q2/Q3); production tRPC router (Q4). Minor residual: `slotEndT","model":"claude","vendor":"claude"}
{"findingId":"f-17","label":"NEEDS_INVESTIGATION","confidence":72,"reasoning":"Q1/Q3: cited 3 lines set a placeholder when refresh_token is absent, but the impact claim (callers persist the literal \"refresh_token\" into DB) depends on unseen artifacts — whether this is the success branch of SafeParseReturnType, what the function returns, and how callers consume .data. The snippet alone can't confirm a real defect vs. an intentional sentinel; need the full parseRefreshTokenResponse body and a caller that stores .data.refresh_token.","model":"claude","vendor":"claude"}
{"findingId":"f-18","label":"TP","confidence":90,"reasoning":"Q1: variable named isTeamAdminOrOwner is assigned `isTeamAdmin && isTeamOwner`; A&&B can never equal A||B for distinct role checks, so the operator contradicts the intent encoded in the snippet itself—either always-false (mutually exclusive roles) or denies plain admins (if isTeamAdmin already includes owners). Runtime authorization defect in a production tRPC handler (Q4 prod). Confirmable from the snippet alone (name vs operator), so TP not NI.","model":"claude","vendor":"claude"}
{"findingId":"f-19","label":"NEEDS_INVESTIGATION","confidence":70,"reasoning":"Q2 (upstream) drives this: line 75 `!blacklistedGuestEmails.includes(guest)` is a case-sensitive exact match, but the bypass only exists if `guests` are NOT lowercased upstream and `blacklistedGuestEmails` IS lowercased. Neither the input zod schema (may `.toLowerCase()` guest emails) nor the blacklist construction is shown. Need the addGuests input schema + blacklist definition; can't confirm exploitability from this snippet, so not TP.","model":"claude","vendor":"claude"}
{"findingId":"f-20","label":"TP","confidence":80,"reasoning":"Q1 confirmed from cited code: two consecutive `def self.downsize` in the same class scope (lines 143-149). Ruby has no arity overloading—the second definition silently replaces the first, making the `\"#{max_width}x#{max_height}\"` variant dead code and discarding its formatting logic. This is a verifiable correctness defect (Lint/DuplicateMethods class), not a style nit, and confirmable from the snippet alone without unseen context.","model":"claude","vendor":"claude"}
{"findingId":"f-21","label":"NEEDS_INVESTIGATION","confidence":70,"reasoning":"Q3 (downstream) unconfirmable: cited line `var maxSizeKB = 10 * 1024` is plainly hardcoded, but the one-line snippet shows neither how maxSizeKB feeds upload validation/the i18n message, nor any removed `Discourse.SiteSettings['max_'+type+'_size_kb']` line to substantiate the 'now hardcodes...regardless' regression, nor whether `type` is in scope. Need the surrounding function and the diff's removed lines; defect is plausible but not provable from this line alone.","model":"claude","vendor":"claude"}
{"findingId":"f-22","label":"TP","confidence":78,"reasoning":"Q1 holds on the snippet itself: an identical `<div class='footer'>%{respond_instructions}</div>` appears inside `if context_posts.present?` (line 11) AND unconditionally after the `<hr>` (line 27). When context_posts is present both render, and `%{respond_instructions}` interpolation fills every occurrence → duplicated footer in the email. Identical content+`footer` class (a 'footer' placed at the top) signals accidental duplication, not an intentional top/bottom design; no unseen code is needed","model":"claude","vendor":"claude"}
{"findingId":"f-23","label":"TP","confidence":74,"reasoning":"embed.js:17 gates a postMessage handler with `discourseUrl.indexOf(e.origin) === -1` — substring containment where exact origin equality is required (Q1: weak check present as written; Q2: e.origin is attacker-controllable). The description's suffix-subdomain example is backwards (indexOf can't match a longer string), but a real prefix/substring bypass exists (e.g. https://forum.example.co ⊂ https://forum.example.com); the underlying defect and the exact-equality fix are real, so the bad example","model":"claude","vendor":"claude"}
